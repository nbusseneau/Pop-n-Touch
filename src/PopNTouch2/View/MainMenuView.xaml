<UserControl x:Class="PopNTouch2.View.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:ViewModel="clr-namespace:PopNTouch2.ViewModel"
             xmlns:s="http://schemas.microsoft.com/surface/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    
    <s:ScatterView ItemsSource="{Binding Songs}" HorizontalAlignment="Center" VerticalAlignment="Center">
        <s:ScatterView.Resources>
            <DataTemplate DataType="{x:Type ViewModel:SongVM}">
                <s:ScatterViewItem Background="Green">
                    <s:SurfaceButton Content="{Binding Song.Title}" Command="{Binding SelectSong}" Background="LightBlue" Width="180" Height="40" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </s:ScatterViewItem>
            </DataTemplate>
        </s:ScatterView.Resources>
        <s:ScatterView.ItemContainerStyle>
            <Style TargetType="{x:Type s:ScatterViewItem}">
                <Setter Property="Height" Value="50"/>
                <Setter Property="Width" Value="200"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CanScale" Value="False"/>
                <Setter Property="CanMove" Value="True" />
                <Setter Property="CanRotate" Value="True" />
            </Style>
        </s:ScatterView.ItemContainerStyle>
    </s:ScatterView>
</UserControl>
